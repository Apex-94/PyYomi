name: Auto Version Tag (Deprecated)

on:
  workflow_dispatch:

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Explain replacement workflow
        run: echo "Automatic version tagging is disabled. Use the manual Release Electron workflow instead."
